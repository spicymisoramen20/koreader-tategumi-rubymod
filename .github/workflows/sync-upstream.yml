name: Sync from upstream koreader

on:
  schedule:
    - cron: '0 9 * * 5'  # Friday at 09:00 UTC (18:00 JST)
  workflow_dispatch:

concurrency:
  group: sync-upstream
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"

      - name: Fetch upstream
        run: |
          git remote add upstream https://github.com/koreader/koreader.git
          git fetch upstream master

      - name: Merge from upstream koreader
        run: |
          resolve_ours() {
            file_path="$1"
            stage2="$(git ls-files -u -- "$file_path" | awk '$3 == 2 { print $1 " " $2; exit }')"
            if [ -z "$stage2" ]; then
              return 0
            fi

            mode="${stage2%% *}"
            sha="${stage2#* }"
            if [ "$mode" = "160000" ]; then
              git update-index --cacheinfo "$mode,$sha,$file_path"
            else
              git checkout --ours -- "$file_path"
              git add "$file_path"
            fi
          }

          restore_our_pins() {
            resolve_ours .gitmodules
            resolve_ours base
            resolve_ours test
          }

          if git merge upstream/master --no-edit; then
            echo "MERGE_FAILED=false" >> $GITHUB_ENV
          else
            restore_our_pins
            REMAINING_CONFLICTS="$(git diff --name-only --diff-filter=U)"
            if [ -z "$REMAINING_CONFLICTS" ]; then
              git commit --no-edit
              echo "MERGE_FAILED=false" >> $GITHUB_ENV
              exit 0
            fi

            # Create a branch with the conflicted state for PR review
            git merge --abort
            BRANCH="upstream-sync-$(date +%Y%m%d)"
            git checkout -b "$BRANCH"
            git merge upstream/master --no-edit || true
            restore_our_pins
            git add -A
            git commit -m "WIP: upstream sync $(date +%Y-%m-%d) — conflicts need resolution"
            git push origin "$BRANCH"
            echo "MERGE_FAILED=true" >> $GITHUB_ENV
            echo "SYNC_BRANCH=$BRANCH" >> $GITHUB_ENV
          fi

      - name: Restore our submodule pins (base, test, and .gitmodules)
        if: env.MERGE_FAILED != 'true'
        run: |
          # ORIG_HEAD is only set when a merge commit was created.
          # If upstream had nothing new, skip this step entirely.
          if ! git rev-parse --verify ORIG_HEAD > /dev/null 2>&1; then
            echo "Already up to date — nothing to restore."
            exit 0
          fi
          CHANGED=0
          git checkout ORIG_HEAD -- .gitmodules base test
          if ! git diff --cached --quiet; then
            CHANGED=1
          fi
          if [ "$CHANGED" = "1" ]; then
            git commit --amend --no-edit
          fi

      - name: Push
        if: env.MERGE_FAILED != 'true'
        run: |
          git fetch origin master
          if ! git merge-base --is-ancestor origin/master HEAD; then
            git rebase origin/master
          fi
          git push origin master

      - name: Create PR on merge conflict
        if: env.MERGE_FAILED == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Upstream sync ${new Date().toISOString().slice(0,10)}: conflicts need resolution`,
              head: process.env.SYNC_BRANCH,
              base: 'master',
              body: `Automatic sync from koreader/koreader failed due to merge conflicts.\n\nThis PR contains the conflicted state — please resolve the markers and merge.\n\nWorkflow run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });
