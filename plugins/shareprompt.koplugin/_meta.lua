local _ = require("gettext")
return {
    fullname = _("Share Prompt"),
    description = _([[Manage reusable prompt templates and share selected text filled into a prompt from the selection menu. Use {{selection}} in a prompt where the selected text should appear.]]),
}
