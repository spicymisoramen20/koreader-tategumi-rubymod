--[[--
Shared highlight geometry knobs (ReaderView + CreHtmlBoxWidget footnote popup).
--]]

return {
    -- Pixels past the base/em band on each cross-axis side (vertical: width;
    -- horizontal: height). One value for book + CRE footnote Lighten.
    CROSS_PAD_PX = 4,
}
